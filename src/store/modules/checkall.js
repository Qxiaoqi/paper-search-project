// initial state
const state = {
  checked: false,
  checkedId: [],
  // 此处需要在获取数据后提交更新，这里暂时造几个假数据
  checkedArr: [100, 101, 102, 103]
};

// getters
const getters = {};

// actions
const actions = {};

// mutations
const mutations = {};

export default {
  // namespaced: true,
  state,
  getters,
  actions,
  mutations
};
